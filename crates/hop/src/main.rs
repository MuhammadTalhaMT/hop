//! CLI entry point. The command-line interface itself is built on top of
//! `hop::config` in a later task; this binary is intentionally minimal
//! until then.

fn main() {
    println!("hop: CLI not implemented yet");
}
